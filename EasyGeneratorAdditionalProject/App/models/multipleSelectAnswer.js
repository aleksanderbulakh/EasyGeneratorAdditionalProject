define(function () {
    function MultipleSelectImageAnswer(spec) {
        this.id = spec.id;
        this.text = spec.text;
        this.isAnswer = spec.isAnswer;
    }

    return MultipleSelectImageAnswer;
});